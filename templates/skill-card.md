# Skill Card

Purpose: define one reusable AI SOP inside a workflow.

```text
Skill name:

Purpose:

Workflow it supports:

When to use:

Inputs needed:

Context required:

Procedure:

Tools or files used:

Output format:

Quality standard:

Human review points:

Do rules:

Don't rules:

Failure modes:

Example input:

Example output:
```

